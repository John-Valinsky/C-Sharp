# 12. Write a program to demonstrate difference between two dimensional and jagged arrays. 

using System;

class Program
{
    static void Main()
    {
        int[,] arr2D = {
            {1, 2, 3},
            {4, 5, 6}
        };

        Console.WriteLine("Two-Dimensional Array:");

        for (int i = 0; i < arr2D.GetLength(1); j++)
        {
            Console.Write(arr2D[i, j] + " ");
        }
        Console.WriteLine();
    }

    int[][] jagged = new 
}