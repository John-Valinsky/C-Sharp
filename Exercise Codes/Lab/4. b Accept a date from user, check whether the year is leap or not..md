# Accept a date from user, check whether the year is leap or not.
=================================================================
using System;
using System.Windows.Forms;

private void btnCheckLeap_Click(object sender, )